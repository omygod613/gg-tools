# MySQL deployment values
# Override values for the mysql chart dependency

mysql:
  auth:
    rootPassword: "root_password"
    database: "my_database"
    username: ""
    password: ""
    replicaCount: 1

  architecture: standalone
