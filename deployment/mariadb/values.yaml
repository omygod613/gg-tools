# MariaDB deployment values
# Override values for the mariadb chart dependency

mariadb:
  auth:
    rootPassword: "root_password"
    database: my_database
    username: ""
    password: ""
    replicaCount: 1

  architecture: standalone
